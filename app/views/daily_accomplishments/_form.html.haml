= form_for @daily_accomplishment do |f|
  - if @daily_accomplishment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@daily_accomplishment.errors.count, "error")} prohibited this daily_accomplishment from being saved:"
      %ul
        - @daily_accomplishment.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :content
    = f.text_area :content
  .actions
    = f.submit 'Save'
